import { RP_ID, RP_ORIGIN, RP_NAME } from "$env/static/private";

export const rpId = RP_ID;
export const rpOrigin = RP_ORIGIN;
export const rpName = RP_NAME;
export const timeoutMilliseconds = 5 * 60 * 1000; // 5 minutes
export const supportedAlgorithmIds = [-7, -257];